- 现象：重启服务器后，因为nginx没有正确启动，导致memos也启动失败
- 查看所有容器（包括已停止的容器）：
```bash
docker ps -a
```
- 重启memos容器：
```bash
docker start memos
```
- 检查容器日志：
```bash
docker logs memos
```
- 设置容器的重启策略（适用于已经创建的容器）
```bash
docker update --restart=always memos
```